# /* vim: set ft=sh: */

[[ -s "$HOME/.sh.d/home-local-bin.sh" ]] && . "$HOME/.sh.d/home-local-bin.sh"
[[ -f "$HOME/.sh.d/.console_color_conf" ]] && . "$HOME/.sh.d/.console_color_conf"
[[ -f "$HOME/go/env" ]] && . "$HOME/go/env"
[[ -f "$HOME/.cargo/env" ]] && . "$HOME/.cargo/env"
